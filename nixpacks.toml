[phases.setup]
nixPkgs = ["nodejs-18_x"]

[phases.install]
cmds = ["echo 'No installation required for static site'"]

[phases.build]
cmds = ["node inject-config.js"]

[start]
cmd = "npx serve -s . -l tcp://0.0.0.0:$PORT"

